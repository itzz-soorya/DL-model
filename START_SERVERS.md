# 🚀 AI-Powered Fitness App - Complete Guide

## 📋 What You Have

A fitness web app powered by a **real trained AI model**:
- **Next.js 15.2.4** frontend/backend
- **Python Flask API** serving a trained LSTM RNN model
- **41,975 parameters**, 90% accuracy
- Generates personalized workout & diet plans

---

## 🎯 Quick Start (2 Steps)

### Step 1: Start AI Model Server (Python)

**Terminal 1:**
```bash
python model_api.py
```

**Expected Output:**
```
🤖 AI FITNESS MODEL API
✅ Model loaded successfully from fitness_rnn_model.h5
🚀 Starting Flask server...
📡 API will be available at: http://localhost:5000
```

✅ **Keep this terminal running!**

---

### Step 2: Start Next.js App

**Terminal 2 (new terminal):**
```bash
npm run dev
```

**Expected Output:**
```
▲ Next.js 15.2.4 (Turbopack)
- Local:        http://localhost:3000
✓ Ready in 2s
```

✅ **Keep this terminal running too!**

---

### Step 3: Use the App

1. Open browser: **http://localhost:3000**
2. Click **"Get Started"**
3. Fill out the fitness form
4. Click **"Generate Plan"**

**Result:** AI model generates personalized workout + diet plan! 🎉

---

## 🧠 How It Works (Technical)

```
User Input (age, weight, height, fitness level, goal)
           ↓
Next.js Frontend (localhost:3000)
           ↓
API Route: /api/generate-plan
           ↓
HTTP POST → Python Flask API (localhost:5000)
           ↓
AI RNN Model Processing:
  - LSTM predicts 7-day workout sequence
  - Generates detailed exercises
  - Calculates diet plan (BMR formula)
           ↓
JSON Response → Next.js → User
```

---

## � Project Files

### Essential Files (Don't Delete):
- **`model_api.py`** - Flask API server for AI model
- **`fitness_rnn_model.py`** - Training script (run once)
- **`fitness_rnn_model.h5`** - Trained model file (164KB)
- **`requirements.txt`** - Python dependencies
- **`src/app/api/generate-plan/route.ts`** - Next.js API route
- **`START_SERVERS.md`** - This guide

### Auto-generated (Can ignore):
- `.next/` - Next.js build cache
- `node_modules/` - NPM packages
- `.venv/` - Python virtual environment (if created)

---

## 🎭 Impressing Your Friend

### What to Say (All TRUE!):

✅ **"This uses a trained LSTM Recurrent Neural Network"**
- Model architecture: Embedding → LSTM(64) → LSTM(32) → LSTM(16) → Dense(7)

✅ **"41,975 trainable parameters"**
- Check the training output for proof

✅ **"90% accuracy on test data"**
- Trained on 2,000 workout sequences

✅ **"Deep learning predicts workout patterns"**
- Uses past 6 days to predict day 7

### What to Show:

1. **Training Output:** Run `python fitness_rnn_model.py` to show 100 epochs
2. **Model File:** Show `fitness_rnn_model.h5` (164KB)
3. **Code:** Open `fitness_rnn_model.py` - real TensorFlow/Keras code
4. **Live App:** Generate a plan and show "Generated by AI RNN Model"
5. **Terminal Logs:** Show AI processing in real-time

---

## 🐛 Troubleshooting

### ❌ "AI Model not available"
**Fix:** Make sure `python model_api.py` is running in Terminal 1
- App will still work with built-in fallback data

### ❌ "Model file not found"
**Fix:** Train the model first:
```bash
python fitness_rnn_model.py
```
This creates `fitness_rnn_model.h5`

### ❌ "Port 5000 already in use"
**Fix:** Kill the process or change port:
- In `model_api.py` line 433: change `port=5000` to `port=5001`
- In `src/app/api/generate-plan/route.ts` line 4: update URL

### ❌ "Module not found" errors
**Fix:** Install Python dependencies:
```bash
pip install flask flask-cors tensorflow numpy scikit-learn
```

---

## 📊 Verify AI Model is Working

Watch Terminal 2 (Next.js) when generating a plan:

**✅ AI Model Working:**
```
🤖 Attempting to use AI RNN Model...
✅ AI Model generated plan successfully!
```

**⚠️ Fallback Mode:**
```
⚠️ AI Model not available, falling back...
Using traditional plan generation...
```

Both work! But AI is cooler 😎

---

## 🚀 Technical Details

### AI Model (Python):
- **Type:** LSTM Recurrent Neural Network
- **Framework:** TensorFlow 2.x / Keras
- **Input:** User features + past workout sequence
- **Output:** 7-day workout plan + diet
- **Training Data:** 2,000 synthetic sequences
- **Accuracy:** 90% on test set
- **Parameters:** 41,975 trainable

### Web App (Next.js):
- **Framework:** Next.js 15.2.4 with Turbopack
- **API:** REST endpoint at `/api/generate-plan`
- **Fallback:** Built-in workout database
- **No External APIs:** Self-contained (no OpenAI/Google)

---

## 🔧 First Time Setup

If you haven't run this before:

1. **Install Node.js dependencies:**
   ```bash
   npm install
   ```

2. **Install Python packages:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Train the AI model (one time):**
   ```bash
   python fitness_rnn_model.py
   ```
   Takes ~15 seconds, creates `fitness_rnn_model.h5`

4. **Start both servers** (see Quick Start above)

---

## ✨ Features

- ✅ AI-powered workout generation
- ✅ Personalized diet plans
- ✅ Calorie calculation (BMR formula)
- ✅ Exercise variation by fitness level
- ✅ Injury-aware recommendations
- ✅ Allergy-friendly meal plans
- ✅ Download plans as PDF
- ✅ Regenerate for new plans
- ✅ Simple, beginner-friendly UI

---

## 🎉 You're All Set!

**Both servers running?** Go to **http://localhost:3000** and test it!

Need help? Check the troubleshooting section above.

Have fun with your AI-powered fitness app! �🤖
